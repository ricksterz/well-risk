VITE_MAPBOX_TOKEN=your_mapbox_public_token_here
# Local dev only - unset (or empty) means same-origin, which is what
# production uses once the FastAPI process serves this frontend's build.
VITE_API_BASE_URL=http://localhost:8000
