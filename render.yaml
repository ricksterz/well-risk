services:
  - type: web
    name: well-risk
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: "8000"
      - key: ALLOWED_ORIGINS
        value: "https://well-risk.onrender.com"
      - key: VITE_MAPBOX_TOKEN
        sync: false
